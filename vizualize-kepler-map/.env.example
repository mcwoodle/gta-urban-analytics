# Mapbox access token for the dev server and multi-file production build.
# Create one at https://account.mapbox.com/access-tokens/.
# The standalone HTML build does NOT need this — it uses a Carto basemap.
MapboxAccessToken=pk.your_token_here
